#!/bin/bash

mkdir -p "${HOME}/bin"

ln -sf ${DOTFILES_ROOT}/bin/sf ${HOME}/bin/sf
ln -sf ${DOTFILES_ROOT}/bin/sip ${HOME}/bin/sip
ln -sf ${DOTFILES_ROOT}/bin/cip ${HOME}/bin/cip
ln -sf ${DOTFILES_ROOT}/bin/essh ${HOME}/bin/essh
ln -sf ${DOTFILES_ROOT}/bin/escp ${HOME}/bin/escp
ln -sf ${DOTFILES_ROOT}/bin/backup ${HOME}/bin/backup
ln -sf ${DOTFILES_ROOT}/bin/freebuffercache ${HOME}/bin/freebuffercache
ln -sf ${DOTFILES_ROOT}/bin/paint ${HOME}/bin/paint
ln -sf ${DOTFILES_ROOT}/bin/xyz ${HOME}/bin/xyz
